{% extends "layouts/email.html.twig" %}

{% block content %}
    <p>{{ __('password_reset_mail_message') }}</p>
    <p><a href="{{ url('password/reset?email=' ~ email ~ '&token=' ~ token) }}">{{ url('password/reset?email=' ~ email ~ '&token=' ~ token) }}</a></p>
    <hr>
    <p>{{ __('ignore_email') }}</p>
{% endblock %}

