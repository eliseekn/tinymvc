{% extends "layouts/email.html.twig" %}

{% block content %}
    <p>The user "{{ user.getName() }}" has registered a new account.</p>
    <p>Click the link below to login to your account:</p>
    <p><a href="{{ url }}">{{ url }}</a></p>
{% endblock %}
