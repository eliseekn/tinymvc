{% extends "layouts/admin.html.twig" %}

{% block page_title %}
	{{ __('RESOURCENAME') ~ ' | Administration' }}
{% endblock %}

{% block page_content %}

    <metrics-cards>
        <metric-card-item 
            title="{{ __('total') }}" 
            icon="fa fa-cog fa-lg" 
            data="{{ RESOURCENAME.getTotalItems() }}"
            columns="4">
        </metric-card-item>
    </metrics-cards>

    <div class="card shadow-sm">
        <div class="card-header">
            <div class="d-flex flex-lg-row flex-column align-items-lg-center justify-content-lg-between">
                <span>{{ __('RESOURCENAME') }}</span>

                <div class="d-flex flex-lg-row flex-column mt-lg-0 mt-2">
                    <span class="mr-md-3">
                        <input type="search" class="form-control" id="filter" placeholder="{{ __('search') }}">
                    </span>

                    <span class="mt-lg-0 mt-2">
                        <a href="{{ route('RESOURCENAME.new') }}" class="btn btn-outline-dark">
                            {{ __('new') }}
                        </a>
                        
                        <export-modal action="{{ route('RESOURCENAME.export') }}">
                            <button class="btn btn-outline-dark mx-2">{{ __('export') }}</button>
                        </export-modal>

                        <button class="btn btn-danger" id="bulk-delete" data-url="{{ route('RESOURCENAME.delete') }}">
                            {{ __('delete') }}
                        </button>
                    </span>
                </div>
            </div>
        </div>

        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover mb-0">
                    <thead>
                        <tr>
                            <th scope="col">
                                <div class="custom-control custom-checkbox">
                                    <input type="checkbox" class="custom-control-input" id="select-all">
                                    <label class="custom-control-label" for="select-all"></label>
                                </div>
                            </th>

                            <th scope="col"><i class="fa fa-sort"></i> ID</th>
                            <th scope="col"><i class="fa fa-sort"></i> {{ __('created_at') }}</th>
                            <th scope="col"></th>
                        </tr>
                    </thead>

                    <tbody>
                        {% for RESOURCENAME in RESOURCENAME.getItems() %}
                        <tr>
                            <td>
                                <div class="custom-control custom-checkbox">
                                    <input type="checkbox" class="custom-control-input" id="{{ RESOURCENAME.id }}" data-id="{{ RESOURCENAME.id }}">
                                    <label class="custom-control-label" for="{{ RESOURCENAME.id }}"></label>
                                </div>
                            </td>

                            <td>{{ RESOURCENAME.id }}</td>
                            <td>{{ date_helper(RESOURCENAME.created_at) }}</td>

                            <td>
                                <a class="btn text-dark p-1" href="{{ route('RESOURCENAME.read', RESOURCENAME.id) }}" title="{{ __('details') }}">
                                    <i class="fa fa-eye"></i>
                                </a>

                                <a class="btn text-dark p-1" href="{{ route('RESOURCENAME.edit', RESOURCENAME.id) }}" title="{{ __('edit') }}">
                                    <i class="fa fa-edit"></i>
                                </a>

                                <confirm-delete 
                                    type="icon" 
                                    title="{{ __('delete') }}"
                                    content='<i class="fa fa-trash-alt"></i>' 
                                    action="{{ route('RESOURCENAME.delete', RESOURCENAME.id) }}">
                                </confirm-delete>
                            </td>
                        </tr>
                        {% endforeach% }
                    </tbody>
                </table>
            </div>
        </div>

        <div class="card-footer d-flex align-items-center justify-content-between">
            <span>{{ __('total_results') }} <span class="font-weight-bold">{{ RESOURCENAME.getTotalItems() }}</span></span>
            <span>{{ __('showing') }}
                <span class="font-weight-bold">{{ RESOURCENAME.getPageTotalItems() == 0 ? RESOURCENAME.getFirstItem() : RESOURCENAME.getFirstItem() + 1 }}</span>
                {{ __('to') }}
                <span class="font-weight-bold">{{ RESOURCENAME.getPageTotalItems() + RESOURCENAME.getFirstItem() }}</span>
            </span>

            {% include "partials/pagination.html.twig" with {pagination: RESOURCENAME} %}
        </div>
    </div>

{% endblock %}